[package]
name = "mettail-examples"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "rhocalc"
path = "../theories/rhocalc.rs"

[dependencies]
mettail-macros = { path = "../mettail-macros" }
mettail-runtime = { path = "../mettail-runtime" }
moniker = { version = "0.5.0", features = ["moniker-derive"] }
